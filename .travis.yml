language: python

python:
  - "2.7"


install:
  - "pip install --quiet coveralls"
  - "pip install --quiet -r requirements/local.txt"

script: coverage run --source=cuble --omit="cuble/suit/*" ./cuble/manage.py test cuble/ --settings=cuble.settings.test

after_success:
  - coveralls